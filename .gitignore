# Godot 4 ignores
.godot/
.import/
export.cfg
export_presets.cfg
*.translation

# Mono-specific ignores (未使用だが念のため)
.mono/

# System / editor files
.DS_Store
.vscode/
.idea/
*.swp
*~

# Build artifacts
build/
export/
dist/

# Local config (LLM endpoint override 等)
data/config.local.json

# Simulation run logs (Phase 4.5 以降は SQLite、jsonl は Phase 4 以前の遺産)
runs/

# SQLite data files (Phase 4.5 以降)
*.db
*.db-journal
